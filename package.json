{
  "name": "vivychat-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend-cloudflare",
    "frontend"
  ],
  "scripts": {
    "install:all": "npm install",
    "dev:backend": "npm run dev --prefix backend-cloudflare",
    "dev:frontend": "npm run dev --prefix frontend",
    "build:backend": "npm run build --prefix backend-cloudflare",
    "build:frontend": "npm run build --prefix frontend"
  }
}
